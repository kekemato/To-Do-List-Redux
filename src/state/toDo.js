const INITIAL_STATE = {
    allToDos: [],
    visibleToDos: [],
    filterToDo: '',
    newToDo: ''
}

export default (state=INITIAL_STATE, action) => {
    switch (action.type) {
        default:
        return state
    }
}